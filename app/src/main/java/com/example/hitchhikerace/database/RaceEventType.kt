package com.example.hitchhikerace.database

enum class RaceEventType {
    START,
    FINISH,
    CAR_START,
    CAR_FINISH,
    RUN,
    ORIENTATION,
    TEAM_MEATING,
    REST_START,
    REST_FINISH,
    ESCAPE,
    CHECKPOINT,
    CUSTOM;
}